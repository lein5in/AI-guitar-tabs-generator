## Day 1 — Project Setup
**Date:** October 20, 2025  

### Summary
- Defined the project goal: creating an AI that converts guitar and bass audio into tablature.  
- Created the GitHub repository and linked it to the local project folder.  
- Set up a Python virtual environment to isolate dependencies.  
- Installed core libraries required for audio processing and AI development (`librosa`, `numpy`, `torch`, `scipy`, etc.).  
- Added a `.gitignore` file to exclude unnecessary or large files (virtual environments, cache, logs, audio data).  
- Reviewed how virtual environments and dependency tracking work using `requirements.txt`.  

### Next Steps
- Implement the initial audio processing module to extract frequencies and notes from input audio files.  
- Test the system with basic `.wav` samples before integrating AI-based analysis and tablature generation.
